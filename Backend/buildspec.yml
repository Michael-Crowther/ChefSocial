version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20
  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region us-west-1 | docker login --username AWS --password-stdin YOUR_ACCOUNT_ID.dkr.ecr.us-west-1.amazonaws.com
  build:
    commands:
      - echo "Building the Docker image..."
      - docker build -t rust-image .
      - docker tag rust-image:latest 831511836258.dkr.ecr.us-west-1.amazonaws.com/simple-docker-service-06cf555716e1:latest
  post_build:
    commands:
      - echo "Pushing the Docker image..."
      - docker push 831511836258.dkr.ecr.us-west-1.amazonaws.com/simple-docker-service-06cf555716e1:latest

artifacts:
  files: []
